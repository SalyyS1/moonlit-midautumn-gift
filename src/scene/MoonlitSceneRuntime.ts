import { ScrollWorldRuntime, type SceneRuntimeOptions } from './ScrollWorldRuntime';

// Stable public name kept for the existing scene facade and smoke test.
export class MoonlitSceneRuntime extends ScrollWorldRuntime {}
export type { SceneRuntimeOptions };
